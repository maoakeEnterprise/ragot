[project]
name = "rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "pydantic",
    "transformers",
    "langchain-text-splitters",
    "fire",
    "torch"
]

[dependency-groups]
dev = [
    "pytest",
    "flake8",
    "mypy"
]
